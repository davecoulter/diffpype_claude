# collab_scratch

Untracked scratch space shared between the user and Claude Code — drafts, one-off artifacts (e.g. demo HTML pages), handoff files.

Nothing in this directory is version-controlled except this file and `.gitignore`. Treat everything else here as disposable — it can be deleted at any time without affecting the project.

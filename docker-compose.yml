services:
  db:
    build:
      context: .
      dockerfile: docker/db.Dockerfile
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - diffpype_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - diffpype_net

  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - diffpype_net

  api:
    build:
      context: .
      dockerfile: docker/api.Dockerfile
    environment:
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      REDIS_URL: redis://redis:6379/0
      TEST_DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/diffpype_test
    ports:
      - "${API_PORT:-8000}:8000"
    volumes:
      - ./src:/app/src
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - diffpype_net

  worker_light:
    build:
      context: .
      dockerfile: docker/worker.Dockerfile
    environment:
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      REDIS_URL: redis://redis:6379/0
      CELERY_QUEUES: light
      CELERY_CONCURRENCY: ${CELERY_LIGHT_CONCURRENCY:-4}
      CELERY_MAX_MEMORY_PER_CHILD: ${CELERY_LIGHT_MAX_MEMORY_PER_CHILD:-200000}
    deploy:
      resources:
        limits:
          memory: ${WORKER_LIGHT_MEM_LIMIT:-512m}
    volumes:
      - ./src:/app/src
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - diffpype_net

  worker_heavy:
    build:
      context: .
      dockerfile: docker/worker.Dockerfile
    environment:
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      REDIS_URL: redis://redis:6379/0
      CELERY_QUEUES: heavy_memory
      CELERY_CONCURRENCY: ${CELERY_HEAVY_CONCURRENCY:-1}
      CELERY_MAX_MEMORY_PER_CHILD: ${CELERY_HEAVY_MAX_MEMORY_PER_CHILD:-8000000}
    deploy:
      resources:
        limits:
          memory: ${WORKER_HEAVY_MEM_LIMIT:-8g}
    volumes:
      - ./src:/app/src
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - diffpype_net

  flower:
    image: mher/flower:2.0.1
    environment:
      CELERY_BROKER_URL: redis://redis:6379/0
      FLOWER_PORT: ${FLOWER_PORT:-5555}
    ports:
      - "${FLOWER_PORT:-5555}:5555"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - diffpype_net

  portainer:
    image: portainer/portainer-ce:latest
    ports:
      - "${PORTAINER_PORT:-9000}:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    networks:
      - diffpype_net

  ui:
    build:
      context: .
      dockerfile: docker/ui.Dockerfile
    environment:
      VITE_API_URL: ${VITE_API_URL:-http://localhost:8000}
    ports:
      - "${UI_PORT:-5173}:5173"
    volumes:
      - ./src/ui:/app
      - diffpype_ui_node_modules:/app/node_modules
    depends_on:
      - api
    networks:
      - diffpype_net

networks:
  diffpype_net:
    name: diffpype_net

volumes:
  diffpype_db_data:
  diffpype_ui_node_modules:
  portainer_data:
